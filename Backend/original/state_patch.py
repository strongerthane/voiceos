"""
State-patch working notes — moved to state_patch.md.

This file was originally a bilingual (EN/中文) markdown progress note that
had been saved with a .py extension, which broke every `compileall` sweep
of the folder. The note content is preserved verbatim in state_patch.md
(2026-09-24); this pointer keeps the .py namespace parseable.

The state management the note describes is implemented in state.py
(CompactState: mode, active step, status counts, bounded history).
"""

# Intentionally no executable code: this file is a pointer, not a module.
